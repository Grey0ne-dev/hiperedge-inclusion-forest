# Compatibility wrapper. The real library build lives at the repository root.

.PHONY: all lib static shared example tests benchmark sanitize install clean

all lib static shared example tests benchmark sanitize install clean:
	$(MAKE) -C ../.. $@
